import { StyleSheet } from "react-native";

export const s = StyleSheet.create({
  root: {
    alignItems: "center",
    justifyContent: "center",
  },
  avatar: {
    alignItems: "center",
    justifyContent: "center",
    borderWidth: 0.5,
  },
  text: {
    fontWeight: "600",
  },
});
